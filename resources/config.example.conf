PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

# ---- Default Values ----
MODEL="mistral"
SYSTEM_PROMPT_PATH="$PROJECT_ROOT/system-prompts/_default.txt"
SKILLS_REPORT_PATH="$PROJECT_ROOT"
TOOLS_DIR="$PROJECT_ROOT/tools"